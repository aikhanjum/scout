# dependencies and build output
node_modules/
dist/
.pio/
.venv/
__pycache__/
*.pyc

# secrets and OS noise
.env
.DS_Store

# recorded runs are outputs. The sample stays in git; whitelist a demo run with a ! line.
data/runs/*.ndjson
!data/runs/room-scan.ndjson

# playwright screenshots
.playwright-mcp/

# diagnostic dumps
pi/tools/scan-dump.json

# Claude skills tooling
.agents/
skills-lock.json
data/live/
data/fonts/
